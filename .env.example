# GraphQL endpoint for Relay (read operations). Defaults to /graphql if unset.
# Example when API serves GraphQL at /graphql:
# VITE_GRAPHQL_URL=http://localhost:3001/graphql
VITE_GRAPHQL_URL=

# Supabase (frontend: auth + optional direct client; API uses these for server-side store + JWT verify)
# Get these from Supabase project: Settings > API > Project URL and service_role key.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
