import { Heading, Stack, Text } from '@design-system'

export function WorkspaceViewsScreen() {
  return (
    <Stack gap={4}>
      <Heading level={2}>Workspace views</Heading>
      <Text muted>Saved views across the workspace.</Text>
    </Stack>
  )
}
