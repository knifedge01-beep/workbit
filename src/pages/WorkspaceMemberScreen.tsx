import { Heading, Stack, Text } from '@design-system'

export function WorkspaceMemberScreen() {
  return (
    <Stack gap={4}>
      <Heading level={2}>Member</Heading>
      <Text muted>Workspace members and invitations.</Text>
    </Stack>
  )
}
