import { PageHeader, Stack } from '@design-system'

export function WorkspaceMemberScreen() {
  return (
    <Stack gap={4}>
      <PageHeader title="Member" summary="Workspace members and invitations." />
    </Stack>
  )
}
